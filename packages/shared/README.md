# packages/shared

Shared types, constants, and utilities used across Next-IOT services.

Currently a placeholder — will be populated as cross-service contracts are defined (e.g. event schemas, API DTOs).
